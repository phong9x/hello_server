/*
 * Created on 20 thg 12 2016 ( Time 10:30:21 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.trams.hello.web.controller.company;

import java.util.HashMap;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.ResponseStatus;
import org.trams.hello.business.service.UserService;
import org.trams.hello.rest.common.AbstractRestController;

@RequestMapping("/v1/public/ajax")
@Controller
public class PublicAjaxControlor extends AbstractRestController {
	@Resource
	private UserService userService;

    @RequestMapping(value = "/ajax_company_member_get_sub_business", method = {RequestMethod.POST}, produces = MediaType.APPLICATION_JSON_VALUE)
    @ResponseStatus(HttpStatus.OK)
    @ResponseBody
    public HashMap<String, Object> ajax_company_member_get_sub_business( 
    		@RequestParam(value = "username") String username,
    		HttpSession session, HttpServletRequest httpRequest) {
        try {
        	HashMap<String, Object> ret = new HashMap<>();
        	int count = userService.countUserExitsByUsername(username);
        	if (count == 0) {
        		ret.put("result", 1);
			} else {
				ret.put("result", 2);
			}
			
            return SUCCESS(ret);
        } catch (Exception e) {
            e.printStackTrace();
            return FAIL();
        }
    }
}
