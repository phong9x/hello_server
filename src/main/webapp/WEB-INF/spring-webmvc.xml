<?xml version="1.0" encoding="UTF-8"?>
<!-- File generated by Telosys Tools Generator ( version 2.1.1 ) - Date 2016-11-23 ( Time 13:29:54 ) -->

<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/task 
		http://www.springframework.org/schema/task/spring-task-3.0.xsd">

	<context:component-scan base-package="org.trams.hello.web" />
	<context:component-scan base-package="org.trams.hello.rest" />
	<context:component-scan base-package="org.trams.hello.push" />
	<context:component-scan base-package="org.trams.hello.common.job" />

    <bean id="applicationConversionService" class="org.trams.hello.web.convert.ConversionServiceFactoryBean"/>

	<!-- <annotation-driven /> enables the support of annotation configuration for Spring MVC controllers, -->
	<!-- as well as enabling Spring 3 type conversion and formatting support. -->
	<!-- Also, support for JSR-303 Bean Validation is enabled by this tag. -->
    <mvc:annotation-driven conversion-service="applicationConversionService"/>

	<!-- static resources -->
	<mvc:resources mapping="/css/**" location="/css/" />
	<mvc:resources mapping="/img/**" location="/img/" />
	<mvc:resources mapping="/images/**" location="file:/data/opt/hello_image/" />
	<mvc:resources mapping="/script/**" location="/script/" />

	<task:annotation-driven />

    <!-- Enable this for integration of file upload functionality -->
    <bean class="org.springframework.web.multipart.commons.CommonsMultipartResolver" id="multipartResolver"/>

	<!-- Tiles -->
	<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver" id="tilesViewResolver">
		<property name="viewClass" value="org.springframework.web.servlet.view.tiles2.TilesView" />
	</bean>
	<bean class="org.springframework.web.servlet.view.tiles2.TilesConfigurer" id="tilesConfigurer">
		<property name="definitions">
			<list>
				<value>/WEB-INF/layouts/layouts.xml</value>
				<value>/WEB-INF/views/**/views.xml</value>
			</list>
		</property>
	</bean>

    <!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources -->
    <mvc:resources location="/, classpath:/META-INF/web-resources/" mapping="/resources/**"/>

	<mvc:default-servlet-handler/>

	<!-- Selects a static view for rendering without the need for an explicit controller -->
    <mvc:view-controller path="/" view-name="index"/>
    <mvc:view-controller path="/uncaughtException"/>
    <mvc:view-controller path="/resourceNotFound"/>
    <mvc:view-controller path="/dataAccessFailure"/>

    <!-- This bean resolves specific types of exceptions to corresponding logical - view names for error views.
    The default behaviour of DispatcherServlet - is to propagate all exceptions to the servlet 
    container: this will happen - here with all other types of exceptions. -->

	<mvc:interceptors>
		<bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
			<property name="paramName" value="lang"/>
		</bean>
		<mvc:interceptor>
		    <mvc:mapping path="/**"/>
		    <mvc:exclude-mapping path="/images/**"/>
		    <mvc:exclude-mapping path="/theme/**"/>
		    <mvc:exclude-mapping path="/resourceNotFound"/>
		    <mvc:exclude-mapping path="/v1/public/polling/**"/>
		    <bean class="org.trams.hello.web.common.LoggerInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>

	<bean id="localeResolver" class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
		<property name="cookieName" value="locale"/>
		<property name="defaultLocale" value="ko" /> <!--change this-->
	</bean>

	<!-- Application Message Bundle -->
	<bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="basename" value="WEB-INF/i18n/messages,WEB-INF/i18n/application"/>
		<property name="fallbackToSystemLocale" value="false"/>
		<property name="cacheSeconds" value="3000"/>
	</bean>

</beans>